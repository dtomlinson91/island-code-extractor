"""Island code extractor for Animal Crossing."""

import panaetius

panaetius.set_config(panaetius.CONFIG, "reddit.secret")
panaetius.set_config(panaetius.CONFIG, "reddit.id")
panaetius.set_config(panaetius.CONFIG, "reddit.user_agent")
panaetius.set_config(panaetius.CONFIG, "reddit.thread_id")
